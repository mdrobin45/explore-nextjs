const UsersPage = () => {
   const err = new Error("Something went wrong!");
   console.log(errs);
   return (
      <div>
         <h2>User page</h2>
      </div>
   );
};

export default UsersPage;
